name: TrueBlocksCore

services:
  core:
    image: trueblocks/core:v0.44.0-beta
    build: ./build/core
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - unchained:/unchained
      - cache:/cache
volumes:
  unchained:
  cache:
